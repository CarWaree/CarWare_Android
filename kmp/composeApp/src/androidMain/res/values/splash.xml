<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Your main application theme -->
    <style name="Theme.App" parent="android:Theme.Material.NoActionBar">
        <!-- Main theme attributes -->
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.MySplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#B30000</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.App</item>
    </style>

    <style name="Theme.CarWare" parent="android:Theme.Material.Light.DarkActionBar" />
</resources>
